<h1>User Profile Page</h1>


<h3>My Projects:</h3>

<%= link_to "Start a New Project", new_project_url %>

<% @projects.each do |list| %>
    <% if @current_user.user_id == project.user.user_id %>
    <%= current_user.project.name %>
    <% end %>
  <% end %>
