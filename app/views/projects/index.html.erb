<%= image_tag("crowd_funder1.png", :alt => "full_logo", :class => "big_logo") %>

<h1>All Projects</h1>

<%= link_to 'New Project', new_project_path %>


<ul>
  <% @projects.each do |project| %>
    <div><%= link_to project.name, project_path(project) %></div>
  <% end %>
</ul>
